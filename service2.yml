---
apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-service
  labels:
    author: Riya
spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30089
  selector:
    app: proxy
...
